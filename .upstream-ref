# manga-image-translator 釘住的參考版本（CLAUDE.md §7）
# 程式碼以「最新 main」為準；模型用各自最新版
# （beta-0.3 release 只是 model host，後續僅少數模型更新，一律取最新）。

repo   = https://github.com/zyddnys/manga-image-translator
branch = main
commit = 441d07c  # 2026-09-26 查核：自 95227a2 起 4 個 commit（#1170 web UI 深色模式、#1171 web 批次佇列
                  # 429 指數退避重試）全在 server/index.html，watched paths 零變更（diff --stat 空；
                  # index.html diff 亦無夾帶 detector/ocr/inpaint/translator 預設值）⇒ 無需 port。
                  # 模型：beta-0.3 資產無更新（detect-20241225.ckpt 仍 2024-12-26）、無新 tag。
                  # 開放 PR：#1148「New wrap」（MERGEABLE 未合）碰 config.py/detection/generic.py/
                  # rendering/custom_openai.py＝新 yolomanga 偵測器（非預設）、SD-ControlNet 去字（GPU）、
                  # font_path、貼邊空 crop 守衛（我方 Ocr.kt 已 clamp）⇒ 合入 main 後再看；#1168 為外部
                  # repo 誤基的巨型 PR（CONFLICTING）不理；#1166／#1043 狀態未變。issue #1163 有人問
                  # detect-20241225.ckpt 授權來源（維護者 0 回覆）⇒ 若另給 grant 需更新我方授權段。
                  # （前次：2026-07-28 efdc229→95227a2 全為 offline MT；2026-08-25 main 靜止、Groq
                  #  退役 id 收進 RETIRED_MODELS；皆無需 port。）
date   = 2026-09-26

# watched paths（最新 layout）：
#   偵測 = manga_translator/detection/default.py (+ default_utils/)  ← DBNet（m-i-t 的 default 偵測器）
#          模型 detect-20241225.ckpt（beta-0.3 release）
#          ※ comic-text-detector（ctd.py + ctd_utils/）已於 2026-07-15 退役、整條移除 ⇒ 不再追
#   OCR  = manga_translator/ocr/  （48px CTC）               ※ M1
#   去字 = manga_translator/inpainting/  （AOT-GAN）          ← v2 去字模型（AOT-GAN 取代 LaMa）
#   翻譯 = manga_translator/translators/chatgpt.py           ※ M2（最新版已含）
#
# 同步紀律：定期對 watched paths 跑  git diff <此 commit>..main，
#           只追 prompt / 參數 / 模型 / provider 變更（§7）。
